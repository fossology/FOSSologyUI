import UploadFromServerClient from "./UploadFromServerClient";

export const metadata = {
    title: "Upload from Server | FOSSology",
};

export default function UploadFromServerPage() {
  return <UploadFromServerClient />;
}