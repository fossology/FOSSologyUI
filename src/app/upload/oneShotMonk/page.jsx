import OneShotMonkClient from "./OneShotMonkClient";

export const metadata = {
    title: "One-Shot Monk | FOSSology",
};

export default function OneShotMonkPage() {
  return <OneShotMonkClient />;
}