import ScheduleAgentsClient from "./ScheduleAgentsClient";

export const metadata = {
    title: "Schedule an Analysis | FOSSology",
};

export default function ScheduleAgentsPage() {
  return <ScheduleAgentsClient />;
}