import DeleteGroupClient from "./DeleteGroupClient";

export const metadata = {
    title: "Delete Group | FOSSology",
};

export default function DeleteGroupPage() {
    return <DeleteGroupClient />;
}