import CreateLicenseClient from "./CreateLicenseClient";

export const metadata = {
    title: "Create Candidate License | FOSSology",
};

export default function CreateLicensePage() {
    return <CreateLicenseClient />;
}