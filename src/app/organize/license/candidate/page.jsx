import CandidateLicenseClient from "./CandidateLicenseClient";

export const metadata = {
    title: "Candidate Licenses | FOSSology",
};

export default function CandidateLicensePage() {
    return <CandidateLicenseClient />;
}