import CreateFolderClient from "./CreateFolderClient";

export const metadata = {
    title: "Create a new FOSSology folder | FOSSology",
};

export default function CreateFolderPage() {
  return <CreateFolderClient />;
}