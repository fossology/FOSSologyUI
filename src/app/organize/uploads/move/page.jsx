import MoveUploadsClient from "./MoveUploadsClient";

export const metadata = {
    title: "Move or Copy Upload | FOSSology",
};

export default function MoveUploadsPage() {
    return <MoveUploadsClient />;
}